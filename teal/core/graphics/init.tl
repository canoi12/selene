local record graphics
    enum DrawMode
        "points"
        "lines"
        "triangles"
    end

    enum TextureFilter
        "nearest"
        "linear"
    end

    enum TextureWrap
        "repeat"
        "clamp_to_border"
        "clamp_to_edge"
    end
end

return graphics